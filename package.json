{
  "scripts": {
    "dev": "live-server --port=3000 --host=127.0.0.1 .",
    "serve": "serve -s .",
    "format": "prettier --write ."
  },
  "devDependencies": {
    "live-server": "^1.2.2",
    "prettier": "^3.6.2",
    "serve": "^14.2.5"
  }
}
